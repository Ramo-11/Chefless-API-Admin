<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      color: #1a1a1a;
      background: #fafafa;
      line-height: 1.7;
      -webkit-font-smoothing: antialiased;
    }
    .container {
      max-width: 720px;
      margin: 0 auto;
      padding: 40px 24px 80px;
    }
    .brand {
      display: flex;
      align-items: center;
      gap: 10px;
      margin-bottom: 32px;
      padding-bottom: 20px;
      border-bottom: 1px solid #e5e7eb;
    }
    .brand-icon {
      width: 40px;
      height: 40px;
      border-radius: 10px;
      display: block;
      object-fit: contain;
    }
    .brand-name {
      font-size: 18px;
      font-weight: 600;
      color: #e8623c;
    }
    h1 {
      font-size: 28px;
      font-weight: 700;
      margin-bottom: 8px;
      color: #111;
    }
    .effective-date {
      font-size: 14px;
      color: #6b7280;
      margin-bottom: 32px;
    }
    h2 {
      font-size: 20px;
      font-weight: 600;
      margin-top: 32px;
      margin-bottom: 12px;
      color: #111;
    }
    p, li {
      font-size: 15px;
      color: #374151;
      margin-bottom: 12px;
    }
    ul {
      padding-left: 24px;
      margin-bottom: 16px;
    }
    li { margin-bottom: 6px; }
    a { color: #e8623c; }
    .contact {
      margin-top: 40px;
      padding: 20px;
      background: #fff7ed;
      border-radius: 10px;
      border: 1px solid #ffedd5;
    }
    .contact p { margin-bottom: 4px; }
    @media (prefers-color-scheme: dark) {
      body { background: #111; color: #e5e7eb; }
      h1, h2 { color: #f9fafb; }
      p, li { color: #d1d5db; }
      .brand-name { color: #f59e0b; }
      .effective-date { color: #9ca3af; }
      .brand { border-bottom-color: #374151; }
      a { color: #fb923c; }
      .contact { background: #2a1e15; border-color: #4a2f1f; }
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="brand">
      <img src="/admin/images/logo.png" alt="Chefless" class="brand-icon">
      <span class="brand-name">Chefless</span>
    </div>
    <%- body %>
  </div>
</body>
</html>
